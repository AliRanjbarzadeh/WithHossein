<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_graph"
    app:startDestination="@id/mobileFragment">

    <fragment
        android:id="@+id/mobileFragment"
        android:name="ir.meysamd.withhossein.app.login.mobile.MobileFragment"
        android:label="MobileFragment"
        tools:layout="@layout/fragment_mobile" />
    <fragment
        android:id="@+id/verifyFragment"
        android:name="ir.meysamd.withhossein.app.login.verify.VerifyFragment"
        android:label="VerifyFragment"
        tools:layout="@layout/fragment_verify" />
</navigation>